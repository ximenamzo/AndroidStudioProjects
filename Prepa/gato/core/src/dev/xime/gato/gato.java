package dev.xime.gato;

import com.badlogic.gdx.ApplicationAdapter;
import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.GL20;
import com.badlogic.gdx.graphics.Texture;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.graphics.g2d.SpriteBatch;
import java.util.List;
import java.util.HashMap;
import java.util.ArrayList;

public class gato extends ApplicationAdapter {

    //private Texture badlogic;
    //private SpriteBatch batch;
    //private int width, height;
    //private float widthBad, heightBad;

    @Override
    public void create() {
        //badlogic = new Texture("badlogic.jpg");
        //batch = new SpriteBatch();

        //width = Gdx.graphics.getWidth();
        //height = Gdx.graphics.getHeight();

        //widthBad = badlogic.getWidth();
        //heightBad = badlogic.getHeight();
    }

    @Override
    public void dispose() {
        //badlogic.dispose();
        //batch.dispose();
    }

    @Override
    public void render() {
        //Gdx.gl.glClearColor(1, 0, 0.5f, 1);
        //Gdx.gl.glClear(GL20.GL_COLOR_BUFFER_BIT);

        //batch.begin();
        //batch.draw(badlogic, width - widthBad, 0, 150, 150);
        //batch.draw(badlogic, 0, height - heightBad, 150, 150);
        //batch.draw(badlogic, width - widthBad, height - heightBad, 150, 150);
        //batch.draw(badlogic, 0, 0, 150, 150);
        //batch.draw(badlogic, 200, 200, 100, 100);
        //batch.end();
    }
}
